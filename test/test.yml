imports:
    - "b/test.yml"

compiler_passes:
    - !require "a/CompilerPass"

parameters:
    foo: "Foo"
    bar: "Bar"
    numbers: 123456

    root: "/some/path"
    child: "/a/b/%root%/another/dir/%root%/"

services:
    a1:
        class: !require "a/a1.js"
        arguments:
            - 5
            - 10
            - 15
        calls:
            - ['hello', ['Foo', 'bar!%child%!!!']]
        tags:
            - 'foobar'
